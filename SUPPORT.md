# Support

Use GitHub issues for reproducible 0.3 bugs and documentation gaps. Include
`gemmafischer version --json`, `doctor --format json`, the exact command, and
redacted output. Never attach a private PGN or the local history database.
Historical pre-recovery behavior is migration research, not a supported-runtime bug.
