# Support

Use GitHub issues for reproducible vNext bugs and documentation gaps. Include `gemmafischer version --json`, `doctor --format json`, the exact command, and redacted output. Historical v2 behavior is migration research, not a supported-runtime bug.

