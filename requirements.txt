# Dependencies for GemmaFischer - Chess LLM Engine + Tutor
# Note: On Apple Silicon you may want to install torch with MPS support as documented in the plan.

# Core ML dependencies
transformers
datasets
accelerate
peft
trl
huggingface_hub
unsloth
torch
torchvision
torchaudio

# Chess and UCI support
python-chess
stockfish  # Python wrapper for Stockfish engine

# Web interface
flask
flask-cors

# Data processing and embeddings
sentence-transformers  # For embedding generation
faiss-cpu  # For vector search (or faiss-gpu if you have CUDA)
pandas
numpy

# PGN processing and chess data
pgnparser  # For PGN file parsing

# Evaluation and testing
pytest
mlflow  # For experiment tracking and monitoring

# Optional dependencies
# bitsandbytes  # For quantization (platform dependent)
# qdrant-client  # Alternative vector database
# opencv-python  # For vision module (if implementing board image recognition)
# pillow  # For image processing
