# Clean Stop and Resume Point

Historical snapshot. The Playwright harness and persisted tutor work listed
below were completed on 2026-09-01. Use [release status](release-status.md) for
the current resume point.

Stopped cleanly on 2026-08-30 after the measurement, correctness, data, and
post-training readiness pass. `gemmafischer status` reports `running: False`.
No Uvicorn, GemmaFischer, MLX generation, or Stockfish process remains.

## Completed in this pass

- Added real Uvicorn/socket runtime qualification with temporary SQLite,
  Stockfish child observation, graceful shutdown, and raw latency evidence.
- Added exact model-output contract grading to the 21-request Gemma profiler and
  aligned its token ceiling with production at 768 tokens.
- Expanded constructed chess accuracy and added exact UCI/SAN/terminal question
  grading with adversarial examples.
- Replaced the obsolete FEN-to-move training path with exact claim-selection
  records, three-way lineage isolation, semantic deduplication/leakage checks,
  and fail-closed post-training readiness.
- Removed seven obsolete data scripts/metadata files whose contracts were no
  longer safe for training.
- Recorded the browser load baseline and updated model, performance, data,
  runtime, training, and execution documentation.

## Verified stop-state gates

```text
Ruff                         passed
strict mypy                  passed, 23 source files
non-model pytest             102 passed, 1 model test deselected
repository audit             passed, 125 files, zero duplicate groups
OpenAPI drift                clean
JavaScript syntax            clean
managed application          stopped
lingering child processes    zero
```

The qualification artifacts were produced from the coherent development
worktree before the integration commit, so they honestly record
`working_tree_clean: false`. Do not relabel them. Regenerate release artifacts
from the next clean release candidate.

## Measured facts

- Runtime: 20 cycles / 80 real HTTP requests; engine p95 310.1 ms, health p95
  3.2 ms, legal p95 2.1 ms; one maximum Stockfish child; zero shutdown orphans.
- Runtime concern: 19 Stockfish restarts under rapid gameplay/review preemption.
- Browser: FCP 44 ms, full load 33.4 ms, four requests, 39,163 transferred bytes;
  LCP was unavailable and remains unclaimed.
- Gemma: 21/21 contract-valid; warm visible TTFT p95 654 ms; warm total p95
  8.83 s; minimum warm decode 27.66 tokens/s; peak MLX 3.47 GB.
- Correctness: 24/24 constructed nonterminal top-1/top-3 checks, 33/33 exact
  question grading examples, 15/15 deterministic and 10/10 full tutoring runs.
- Data smoke: real hash-verified archive and Stockfish build produced 84/10/6
  train/validation/final-test records with zero rejected rows.
- Training: not authorized. Unsloth is not installed; the installed 4-bit Gemma
  quant is not a training source.

## Next ordered work

1. Regenerate the runtime, model, accuracy, tutoring, question, data-audit, and
   readiness artifacts from a clean commit so release evidence has a clean-tree
   provenance bit.
2. Add the checked-in Playwright E2E fixture/viewport matrix. Current browser
   evidence is real but the durable release harness remains open.
3. Reduce Stockfish restart churn before the 100-ply/1,000-request endurance and
   memory-growth runs.
4. Implement the persisted deterministic tutor interaction vertical slice.
   Current questions qualify grading only; they are not a shipped tutor state
   machine or UI.
5. Build and audit 10,000/1,000/1,000 selector rows plus the 1,000-position
   held-out chess suite.
6. Complete blind human usefulness review. Only then select native base weights,
   exact Unsloth/MLX-LM pins, and request authorization for a 7-20-step LoRA
   smoke comparison.

## Resume commands

```bash
git status --short
git log -1 --oneline
uv run gemmafischer verify
uv run gemmafischer repo-audit
uv run gemmafischer doctor --profile full
uv run gemmafischer status
```

Do not start training, install Unsloth, download native weights, or promote Gemma
without the missing data, human, and explicit authorization gates.
